#Create a normal user called scanner with the scanner group with an encrypted password
#password can be generated by the following command:
#if python 3 below(without the hash):
#python3 -c 'import crypt,getpass;pw=getpass.getpass();print(crypt.crypt(pw) if (pw==getpass.getpass("Confirm: ")) else exit())'
#if python 2 below(without the hash):
#python -c 'import crypt,getpass;pw=getpass.getpass();print(crypt.crypt(pw) if (pw==getpass.getpass("Confirm: ")) else exit())'

---
- hosts: linux
  become: yes
  tasks:
     - name: Add a group
       group:
        name: scanner
        state: present

     - name: Add user
       user:
        name: scanner
        group: scanner
        comment: scanner user
        password: $6$zyeMBxmYd5XW8pSg$O1r8vuo8At7CF7N31g5V0wJaQkcAWXhGF0Ie9tS4HNK7YkF4.2bedNQw8SRUEcYmxehnEnR8u0WJUPYrIfy2K0
---


